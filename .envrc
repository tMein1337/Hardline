# direnv: auto-enter the Nix dev shell on `cd` into this repo.
# Requires `direnv` + `nix-direnv`. Safe to delete if you don't use direnv;
# `nix develop` gives the same environment manually.
use flake
